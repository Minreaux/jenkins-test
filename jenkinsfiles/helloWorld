#!/usr/bin/env groovy
library("utils@$BRANCH_NAME")

pipeline
{
    agent any

    stages
    {
        stage('Greeting')
        {
            steps
            {
                sayHello('World')
            }
        }
    }
}
