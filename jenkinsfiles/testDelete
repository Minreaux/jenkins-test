#!/usr/bin/env groovy

// Jenkinsfile that defines a declarative Jenkins pipeline that is used to test Jenkins Job DSL configured job deletions
pipeline
{
    agent any

    stages
    {
        stage('Testing')
        {
            steps
            {
                echo('Testing...')
            }
        }
    }
}
