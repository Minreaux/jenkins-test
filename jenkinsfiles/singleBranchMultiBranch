#!/usr/bin/env groovy

// Jenkinsfile that defines a declarative Jenkins pipeline
pipeline
{
    agent any

    stages
    {
        stage('Branch')
        {
            steps
            {
                echo("branch")
            }
        }
    }
}
